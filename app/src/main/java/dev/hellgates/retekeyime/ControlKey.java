package dev.hellgates.retekeyime;

/**
 * View-local key commands. They change which layer or layout the keyboard shows and never reach
 * the input dispatcher or the editor.
 */
public enum ControlKey {
    SHIFT,
    LAYOUT_TOGGLE
}
