package dev.hellgates.retekeyime;

/**
 * What the symbol layer's right-hand 3x3 block shows. Two lock toggles move between them, like a
 * numeric keypad's Num Lock: digits by default, the arrow/navigation cluster, or the function keys.
 */
public enum NumpadMode {
    NUMBERS,
    ARROWS,
    FUNCTIONS
}
